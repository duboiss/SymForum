php_version: 8.1

rules:
    composer.invalid_file:
        enabled: false
    web.missing_favicon:
        enabled: false
    web.missing_robots_txt:
        enabled: false

global_exclude_dirs:
    - public

exclude_patterns:
    - docker-compose.yml
