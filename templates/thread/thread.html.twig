{% extends 'base.html.twig' %}

{% block title %}{{ thread.title }} par {{ thread.author.pseudo }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}

    {% if is_granted('ROLE_MODERATOR') %}
        {{ encore_entry_link_tags('modnav') }}
    {% endif %}
{% endblock %}

{% block body %}
    {% if is_granted('ROLE_MODERATOR') %}
        <div class="mod_nav">
            <ul>
                {% if thread.locked %}
                    <li><a href="{{ path('thread.unlock', {'id': thread.id}) }}" title="Ouvrir le sujet"><i
                                    class="fas fa-lock-open"></i></a></li>
                {% else %}
                    <li><a href="{{ path('thread.lock', {'id': thread.id}) }}" title="Fermer le sujet"><i
                                    class="fas fa-lock"></i></a></li>
                {% endif %}
            </ul>
        </div>
    {% endif %}

    <div class="container">
        <div class="row justify-content-md-center mt-5">
            <div class="col col-lg-12 col-md-12 col-sm-12">

                {% for message in messages %}
                    <div class="card bg-secondary mb-4 d-flex" id="{{ message.id }}">
                        <div class="card-header">{{ thread.title }}</div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-lg-2 text-center">
                                    {% set author = message.author %}
                                    {% if author is null %}
                                        Compte supprimé
                                    {% else %}
                                        {{ user_profile_link(message.author) }}

                                        {% set role = user_profile_role(author) %}
                                        {{ role ? role }}
                                    {% endif %}
                                </div>
                                <div class="col-lg-10">{{ message.content|purify }}</div>
                            </div>

                            <div class="row mt-3">
                                <div class="col text-right">
                                    Posté le {{ message.publishedAt|date('d/m/Y à H:i:s') }}
                                    {% if (message.author == app.user) and (message.thread.locked) is null %}
                                        - <a href="{{ path('message.edit', {'id': message.id}) }}">Editer</a>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}

                {% if not thread.locked %}
                    {% if app.user %}
                        <div class="mb-3">
                            <p class="text-center">
                                Connecté en tant que {{ app.user.pseudo }}, vous pouvez répondre :
                            </p>
                            {% include 'message/_form_message.html.twig' %}
                        </div>
                    {% else %}
                        <div class="alert alert-info text-center">
                            <a href="{{ path('security.login') }}">Connectez-vous pour répondre.</a>
                        </div>
                    {% endif %}
                {% else %}
                    {% if app.user %}
                        <div class="alert alert-warning text-center">Ce sujet est verrouillé, vous ne pouvez donc pas y
                            répondre.
                        </div>
                    {% endif %}
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}