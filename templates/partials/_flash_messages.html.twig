{% set icons = {
    'info': 'info-circle',
    'success': 'check-circle',
    'warning': 'exclamation-circle'
} %}

<div style="position: fixed; top: 130px; right: 30px; min-width: 300px;">
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="toast" id="toast-{{ label }}-{{ loop.index }}" role="alert" aria-live="assertive"
                 aria-atomic="true" data-delay="3200">
                <div class="toast-header">
                    <strong class="mr-auto"><i
                                class="fas fa-{{ icons[label] ?? 'times-circle' }}"></i> {{ message['title'] }}</strong>
                    <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="toast-body">
                    {{ message['content'] }}
                </div>
            </div>
        {% endfor %}
    {% endfor %}
</div>