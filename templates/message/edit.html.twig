{% extends 'base.html.twig' %}

{% block title %}Edition de votre message{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-md-center mt-5">
            <div class="col col-lg-12 col-md-12 col-sm-12">
                <div class="mb-3">
                    <p class="text-center">
                        Edition de votre message dans le sujet {{ message.thread.title }}
                    </p>
                    {{ include('message/_form_message.html.twig', {button_label: 'Modifier'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}